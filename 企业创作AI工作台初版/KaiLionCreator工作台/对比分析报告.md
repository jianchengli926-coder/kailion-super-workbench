# 对比分析报告 · WfwCreator vs KaiLionCreator

> 生成时间：2026-09-23
> 对比对象：网蜂窝 WfwCreator Agent v0.3.1.57 ｜ 锴利匠心 AI 工作台 KaiLionCreator v0.1.0
> 资料基础：3 个官方网址 + 安装包 + 已安装程序目录 + 快捷方式文件

---

# 第一部分：资料解析完整度（先回答你的核心问题）

**直说结论：功能规格我拿到了接近 100%，但源代码我拿到了 0%。**

这两件事必须分开看，因为它决定了你和朋友交流时该问什么。

## 1.1 我完全拿到的东西（可以逐条核对）

| 类别 | 拿到什么 | 证据来源 |
|---|---|---|
| 产品定位与卖点 | 4 大核心卖点、22 项功能特性、10 项业务矩阵 | 官网 + 产品页全文 |
| **节点体系（68 个）** | **每一个节点的中文名、英文 id、所属分类、出厂可见性、功能说明** | 教程页「节点库全景」章节（逐条列出） |
| 工作流库 | 20 个分类名称、快速入门区 8 条模板、加载模板的三条必做 | 教程页「工作流库速查」 |
| 资源库 | 14 类资源库的名称、哪 7 个出厂显示、哪 8 个出厂隐藏 | 教程页「资源库与素材」 |
| 界面结构 | 六个区域的职责、智能工作台的三块、历史面板行为 | 教程页「界面六个区域」 |
| 快捷键 | 11 个快捷键的完整定义 | 教程页「快捷键」 |
| 报错自查 | 12+ 条高频问题的排查顺序 | 教程页「报错自查」 |
| 实操工作流 | 10 组链路的「链路 / 参数要点 / 常见坑」原文 | 教程页「小白常见工作流」 |
| 六项进化内核 | 工具工厂 / 智能选 MCP / 深度思考 / 数字人进化 / 任务引擎 / 差距自感知的**机制描述** | 产品页「智能进化内核」 |
| 技术栈 | Tauri v2 + Rust + WebView2 + NSIS，identifier `com.wfw.WfwCreator` | 二进制字符串取证 |
| 数据模型 | 7 个持久化 JSON 文件名 + 数据目录路径 | 二进制字符串取证 |
| **后端接口（17 个）** | 全部 REST 路由，含分桶规则与 `/auto` 自动选模型语义 | 二进制字符串取证 |
| 权限模型 | Tauri ACL 的 window/webview/fs 作用域清单 | 二进制字符串取证 |
| 打包参数 | NSIS v3.11、asInvoker、PerMonitorV2 DPI | 安装包 PE 清单 |

## 1.2 我**没有**拿到的东西（必须直说）

| 没拿到 | 为什么 | 影响 |
|---|---|---|
| **前端源码（HTML/CSS/JS）** | Tauri v2 把前端资源压缩后嵌在 `.rdata` 节区里，`strings` 提取不出任何可读的 HTML/CSS/JS。需要专门写 brotli 解包器 + 可能反编译打包产物 | 我**不知道**它的 DOM 结构、CSS 具体数值、JS 类名与算法实现 |
| **精确的设计令牌** | 同上 | 颜色值、字号、间距、圆角、动效时长都是我按品牌重新设计的，**不是**照抄 |
| 每个节点的 **Prompt 模板** | 在前端源码里 | 各节点的提示词工程逻辑我按业务重新编写 |
| 19 个模型的 **路由表** | 在 Rust 后端 + 前端配置里 | 我按协议族重建了 10 条线路 |
| REST API 的**请求/响应契约** | 只有路由字符串，没有 schema | 我按惯例设计了自己的数据契约 |
| 14 类资源库的**种子内容** | 在用户在本地生成的 JSON 里（安装包里没有） | 我全部换成了阳江刀剪行业资产（自研） |
| **实际界面外观** | 未运行过。它是 Windows x64 程序，本机是 macOS，无法直接运行 | 我的界面是按功能规格**重新设计**的，视觉上与原产品必然不同 |
| 工作流引擎的**调度细节** | 在 Rust 后端 | 我用浏览器内拓扑排序重建 |

## 1.3 所以，准确的回答是

> **是的，我完整掌握了它的「功能规格」——能列出全部 68 个节点、20 类工作流、14 类资源库、6 大界面区域、17 个接口和全部交互规则。**
>
> **但我没有拿到它的「源代码与设计稿」——前端源码是压缩嵌入的，我没有做反编译提取；界面也没有实际运行观察过。**
>
> 换句话说：**我能完整还原「它有什么功能、怎么用」，但不能逐像素还原「它长什么样、代码怎么写的」。**

## 1.4 如果你想要「一比一移植」，需要向你朋友要这 7 样

这是最实际的一条建议。直接开口要这 7 样，比任何逆向都高效：

1. **前端源码仓库**（或 `dist/` 构建产物未压缩版）—— 解决 DOM/CSS/JS 全部问题
2. **节点定义文件**（每个节点的参数 schema + prompt 模板）—— 节点能力对齐的关键
3. **Rust 后端源码**（或 OpenAPI/Swagger 文档）—— 17 个接口的请求响应契约
4. **19 个模型的路由配置文件** —— 模型接入表
5. **14 类资源库的种子 JSON** —— 内容资产
6. **设计令牌 / Figma 稿**（如果他有）—— 视觉 1:1 的前提
7. **明确授权的书面说明**（邮件即可）—— 写明允许二次开发与商用范围，这是保护你也是保护他

> 只要给到第 1 和第 2 项，我就能把界面和节点能力做到接近 1:1。

---

# 第二部分：横向对比（功能模块逐项）

图例：✅ 同构实现 ｜ ⚠️ 部分实现 ｜ ❌ 未实现

## 2.1 框架级能力

| 原产品功能 | 原描述 | KaiLionCreator | 状态 |
|---|---|---|---|
| 可视化节点编辑器 | 45+ 节点拖拽、撤销重做、复制粘贴、自动布局 | 74 节点；缩放平移、端口连线、框选、60 步撤销重做、自动布局、小地图、LOD 分级渲染 | ✅ |
| 双主视图 | 工作台首页 ↔ 经典画布，画布始终挂载 | 智能工作台 ↔ 经典画布，共用同一份画布数据 | ✅ |
| 自定义模式 | 出厂显示 30，其余按需放出 | 出厂显示 36，其余 38 个按需放出，本机永久生效 | ✅ |
| 参数即时生效 | 改完直接运行，无需保存 | 节点内联 + 右侧面板双通道，即时生效 | ✅ |
| 长文本独立编辑器 | 节点内只放预览卡 | 独立编辑器 + 提示词库插入 + 结构模板补全 | ✅ |
| 14 类资源库 | 角色/场景/品牌/风格/专家/数字人/技能/商品/知识/素材/提示词/选题/工作流/语义 | 14 类全有，内容换成阳江刀剪行业资产 | ✅ |
| 数据备份与迁移 | 导出/导入资源库与画布 | 导出/导入全量 JSON（含供应商与定时任务） | ✅ |
| 本地优先 | 数据全本地、断网可用 | 同（localStorage；桌面版可换文件系统） | ✅ |

## 2.2 生成类节点（16 个）

| 原产品 | KaiLionCreator | 状态 | 说明 |
|---|---|---|---|
| 图像生成 8 模型：Banana Pro / GPT Image / 创意灵感 / Banana 快速版 / SeeDream / DALL·E / Flux / Z-Image / Agnes Image | 同 8+1 个节点，参数体系一致（画幅/档位/数量/负向词） | ✅ 框架 | 真实调用目前只实现 **OpenAI 兼容协议**一条；其余走本地推演 |
| 视频生成 8 模型：Seedance / Omni / MiniMax / Grok / Sora / Veo / Kling / Agnes Video | 同 8 个节点，参数含时长/画幅/分辨率/音效/种子 | ⚠️ | 节点与参数齐备，**真实视频 API 未接**（各家协议差异大，需逐个对接） |
| 3D 生成：Wfw3D / AHOLO World / 3D 模型 | 同 3 个节点，含生成模式与 5 种导出格式 | ⚠️ | 同上，未接真实 3D API |

## 2.3 内容与电商（14 个）

| 原产品 | KaiLionCreator | 状态 |
|---|---|---|
| 电商详情页：详情页生成 / 复刻 / 解析 / 印刷定制 / 批量生成 | 同 5 个节点，参数含平台、双语言、模块勾选、屏数 | ✅ 逻辑同构 |
| 带货脚本：六维拆解 + 正交枚举变体 + 完整分镜 | 同（功能/效果/情绪/信任/差异化/转化 × 前三秒钩子） | ✅ |
| 图文生成：5 套平台预设 + 第 k 段对应第 k 图 | 6 套预设（多加 LinkedIn）+ 内容策略/视觉控制/合规档位 | ✅ |
| 详情页解析：13+ 平台竞品复刻 | 同 | ⚠️ 解析需真实抓取服务 |
| 品牌 IP 全案：20+ 模块 | 节点 + 5 个默认模块勾选 + 异步模式 | ⚠️ **20+ 模块未逐一实现** |
| 个人 IP：6 层级 22 模块 | 节点 + 6 层级勾选 | ⚠️ **22 模块未逐一实现** |
| 选题挖掘：跨平台热点 | 节点 + 平台多选 + 时间范围 | ⚠️ 需真实热点 API |
| 视频大纲 / 分镜 / 导演台 / 组装 / 复刻 / 替换 | 6 个节点全有 | ✅ 逻辑同构 |

## 2.4 连接器与工具（16 个）

| 原产品 | KaiLionCreator | 状态 |
|---|---|---|
| LLM 三协议 + 深度思考 + 多轮 + 多模态 | 协议选择 + 深度思考开关 + 最大输出；真实调用实现 OpenAI 兼容 | ⚠️ |
| 内容审查（多模态合规） | 节点 + 4 档 + 广告法违禁词 + 多平台规则库开关 | ⚠️ 无真实规则库 |
| MCP 外部工具 / CLI | 两个节点齐备 | ⚠️ 未实现真实 MCP 客户端 |
| GEO 生成式引擎优化 | GEO 节点 + 独立站文案节点（含 JSON-LD） | ⚠️ 无专用诊断算法 |
| 信息检索 | 节点 + 联网/知识库/仅知识库 + Top K | ⚠️ 需检索供应商 |
| 文档转换 12 格式 | 12 格式齐全 | ✅ **真实转换**：docx / xlsx / pptx / html / md / txt / csv / json / xml / rtf / epub 全部真文件；仅 PDF 需走「打印为 PDF」 |
| 图像转换 / 模型转换 / 宫格分割 | 3 个节点齐备 | ✅ 图像转换与宫格分割**真实执行**（Canvas）；模型转换仍需 3D 库（桌面版） |
| 专家讨论 / 专家协作 / 数字人协作 | 3 个节点，数字人支持顺序/并行/讨论/混合 | ✅ |
| 办公四件套 + PPT 内容 + PPT 组装 | 6 个节点齐备 | ✅ **产出真实 .docx / .xlsx / .pptx 文件**，可用 Office / WPS 打开编辑 |
| 供应链 8 节点（云牛顿 3 + 妙手 4 + 店雷达 1） | 8 个节点齐备 + 人工闸门标记 | ⚠️ 需真实 AppKey/授权 |
| 命理玄学 | 同 | ✅ |

## 2.5 智能内核（6 项进化能力）

| 原产品 | KaiLionCreator | 状态 |
|---|---|---|
| **意图预测引擎**（置信度、槽位校验、自动澄清、参数纠正） | 24 条规则打分 + 12 类槽位抽取 + 真实置信度 + 非法参数自动纠正 + 低置信度澄清 + 建议选项 | ✅ **同构实现** |
| **AI 教练 / 智能分析** | 画布结构体检：孤立节点、缺上游、供应商缺失、协议冲突、闸门提示 → 健康度评分 + 建议 | ✅ **同构实现** |
| AI 工具工厂（缺口探测→生成规格→真跑验证→注册技能） | ❌ | ❌ 缺失 |
| AI 智能选 MCP（意图匹配模板 + 自举兜底） | ❌ | ❌ 缺失 |
| 深度思考模式（分析→规划→执行→反思 + critic） | 只有参数开关，无四阶段流程 | ❌ 引擎缺失 |
| 数字人记忆与进化（持久记忆 + 能力画像 + 品牌契合度闸门） | 只有「启用持久记忆」开关 | ❌ 引擎缺失 |
| 任务引擎与复盘（OKR 拆解 → 推进 → 复盘 → 案例库） | ❌ | ❌ 缺失 |
| 差距自感知与自举（意图聚类 + 阈值触发） | ❌ | ❌ 缺失 |

## 2.6 平台与自动化

| 原产品 | KaiLionCreator | 状态 |
|---|---|---|
| 批量并行执行 | `runParallel` 已实现，并行数可配，单条失败可不中断 | ✅ |
| 定时任务（cron 7×24） | 任务定义 + 启停 + 立即跑一次；cron 表达式已生成 | ⚠️ Web 版不真跑，桌面版可接 |
| 内置 HTTP API 服务器（17 端点） | ❌ | ❌ 缺失（桌面版可加） |
| 飞书机器人协同 | ❌ | ❌ 缺失 |
| 供应商管理 | 10 条出厂线路 + UI 逐条配置 + 测试连通 + 节点绑定表 | ⚠️ 无「导入加密配置文件」 |
| 自动更新 | ❌ | ❌ 缺失 |

---

# 第三部分：纵向对比（实现深度）

横向看「有没有」，纵向看「做到什么程度」。

| 维度 | WfwCreator | KaiLionCreator | 差距评价 |
|---|---|---|---|
| 技术栈 | Tauri v2 + Rust 后端 + WebView2 | 原生 ES Module（无构建步骤） | 我方零依赖、改完即刷；但缺 Rust 侧的原生能力 |
| 前端框架 | 未取得源码（预计 Vue 或 React） | 原生 DOM + 手写组件 | 我方组件复用度低，但体积小、无依赖 |
| 样式体系 | 未取得 | 3 层 CSS + 变量令牌 + 深/浅双主题 | 我方可切换主题（原产品似乎只有深色） |
| 节点渲染 | 未取得 | DOM 节点 + SVG 连线 + LOD 分级渲染 | **无虚拟化**：>200 节点会明显卡顿 |
| 数据存储 | 本地 JSON 文件（7 个文件），无容量上限 | localStorage，约 5MB 上限 | **Web 版是硬伤**，桌面版可换文件系统解决 |
| 执行引擎 | Rust 后端 + HTTP 接口，可并发 | 浏览器内拓扑执行 | 无并发隔离，重任务会阻塞 UI |
| 供应商协议 | 8 图像 + 8 视频 + 3 LLM 全协议 | 真实适配器：OpenAI 兼容（LLM + 图像） | **最大的功能差距** |
| 接口层 | 17 个 REST 端点，可被外部调用 | 无 | 缺失 |
| 测试 | 未知 | 3 套无头 Chrome 回归（26 项断言）+ 语法预检，退出码可做 CI | **我方有可验证的测试资产** |
| 文档 | 官网教程页 + FAQ 页 | **11 章内置说明书**（节点表/工作流表/资源库表从实时数据生成，永不过期）+ 4 份 md | 我方文档在软件内，离线可查 |
| 无障碍 | 未知 | 无 ARIA、无键盘全可达 | ❌ 弱项 |
| i18n | 中文 | 中文 | 未做英文界面 |
| 响应式 | 桌面 | 桌面（最低 1180×720） | 同 |

---

# 第四部分：待办清单（按优先级）

## P0 · 影响「能不能真正干活」

| # | 待办 | 状态 |
|---|---|---|
| 1 | **真实文档导出**（.docx/.xlsx/.pptx） | ✅ **已完成** — 自研 ZIP 写入器 + OOXML 生成器，零依赖，产出真文件 |
| 2 | **真实图像处理**（格式互转 / 缩放 / 宫格切割 / 多图合并 PDF） | ✅ **已完成** — Canvas + 自研 PDF 写入器，全部真跑 |
| 3 | **文档格式互转**（12 种） | ✅ **已完成 11 种** — 仅 PDF 因中文需内嵌 CJK 字体，走「打印为 PDF」替代 |
| 4 | 接真实视频 API（8 个模型） | ⬜ 待做 — 需各家协议逐个对接 |
| 5 | 数据容量（localStorage 5MB 上限） | ⬜ 待做 — 打包 Tauri 换文件系统 |
| 6 | MCP 客户端 | ⬜ 待做 — 实现 MCP over stdio / SSE 最小客户端 |

## P1 · 功能完整度

| # | 待办 | 说明 |
|---|---|---|
| 6 | AI 工具工厂 | 缺口探测 → 生成规格 → 真跑验证 → 注册技能 |
| 7 | AI 智能选 MCP | 意图匹配模板 + 自举兜底 |
| 8 | 深度思考四阶段 | 分析 → 规划 → 执行 → 反思 + critic 自检 |
| 9 | 数字人记忆与进化 | 持久记忆 + 能力画像 + 品牌契合度闸门 |
| 10 | 任务引擎与复盘 | OKR 拆解 + 验收标准 + 案例库 |
| 11 | 差距自感知与自举 | 意图聚类 + 阈值触发 |
| 12 | HTTP API 服务器 | 17 个端点（桌面版用 axum） |
| 13 | 飞书机器人 | 群聊下达创作需求 |
| 14 | 品牌 IP 20+ 模块 / 个人 IP 22 模块逐项实现 | 现在是骨架，需要补模块定义 |
| 15 | 供应商「导入加密配置文件」 | 兼容原产品的 config + 密码导入方式 |

## P2 · 体验与质量

| # | 待办 | 说明 |
|---|---|---|
| 16 | 专家库 12 → 30+ | 补足到与原产品同量级 |
| 17 | 真实内容审查规则库 | 违禁词表 + 平台规则表落地 |
| 18 | GEO 专用诊断算法 | 目前只是 LLM 调用 |
| 19 | 3D 真实 API 接入 | Lux3D / AHOLO |
| 20 | 画布节点虚拟化 | >200 节点性能优化 |
| 21 | 无障碍 | ARIA + 键盘全可达 |
| 22 | 英文界面 | i18n |

---

# 第五部分：本次代码审查与修复记录

我对自己的代码做了完整走查，发现并修复了 **4 个真实功能缺陷** + 7 处隐患。

## 5.1 修复的真实缺陷（已用自动化测试验证）

| # | 缺陷 | 严重度 | 修复 | 验证结果 |
|---|---|---|---|---|
| 1 | **在任意视图按 Delete 都会删掉画布上选中的节点** | 🔴 数据丢失 | `CanvasView.onKey` 增加「画布不可见则不响应」守卫 | 工作台视图按 Delete：节点 4→4 ✅ 未误删；切回画布按 Delete：4→3 ✅ 正常 |
| 2 | **上游文本经 `text` 端口传入时，文档/LLM 节点收不到内容**（适配器只认 `prompt`/`in`） | 🟠 功能失效 | 执行引擎增加通用取值 `inputs._any`，所有适配器统一读取 | 规格表 / LLM / Word 三个下游节点全部收到上游文本 ✅ |
| 3 | **参数面板的「点击上传」和「修改勾选项」是两个死按钮**（无任何绑定） | 🟠 功能缺失 | 补齐文件选择器（图片/视频走 ObjectURL 并自动归档进素材库）与多选勾选面板 | 上传按钮触发文件选择器 1 次 ✅；多选按钮打开「详情页生成 · 模块组合」面板 ✅ |
| 4 | **定时任务列表在新建/启停后不刷新**（每次操作重建 DOM 却没替换进弹窗） | 🟡 体验 | 重写为单一持久 body + `render()` 重绘 | 新建后列表 0→1 ✅；启停后标签立即变「暂停」 ✅ |

## 5.2 修复的隐患（7 处）

| # | 隐患 | 修复 |
|---|---|---|
| 5 | LOD 最小档（<40%）节点折叠后端口跑出节点框外 | `.lod-mini .node` 加 `min-height: 62px` |
| 6 | 备份文件含明文 API Key，界面上没有提醒 | 设置页加显式警告 |
| 7 | 智能工作台执行时跑的是 `built` 副本而非画布自身节点，参数可能不一致 | 改为直接执行画布节点 |
| 8 | `autoLayout` 里有一段没用的 `topoSort` 残句 | 删除 |
| 9 | `canvas.js` / `manual.js` 有未使用导入 | 清理 |
| 10 | `globalSearch` 有未使用变量 | 清理 |
| 11 | 素材库上传后侧边栏计数与列表不刷新 | 增加 `klc:assetsChanged` 事件联动 |
| 12 | 说明书里的资源库计数少 1（「工作流」是视图类型未被计入） | 修正口径，现在正确显示 14 类 |

## 5.3 全量回归结果

```
▶ 语法预检…                      ✓ 全部通过
▶ 无头 Chrome 测试：verify        PROBE-1..10 全过，控制台错误 0
▶ 无头 Chrome 测试：verify-bugs   9 项断言全过，控制台错误 0
▶ 无头 Chrome 测试：verify-manual 11 章 / 74 节点行 / 50 工作流，控制台错误 0
▶ 无头 Chrome 测试：verify-files  真实文件产出全过，控制台错误 0
✅ 四套测试全部通过
```

### 真实文件产出的三层验证（不是「跑通了」，是「真能打开」）

| 层 | 方法 | 结果 |
|---|---|---|
| ① 结构 | 自研 ZIP 读取器逐条核对本地头 / 长度 / **CRC32** | ✅ 全部匹配 |
| ② 内容 | 每个 `.xml` / `.rels` 部件用 `DOMParser` 解析 | ✅ 0 个解析错误 |
| ③ 外部工具 | 把文件写到磁盘，用系统工具再验一遍 | ✅ `unzip -t` 报 No errors detected；`textutil` 读出 Word 全部正文与表格；Chrome 内置 PDF 阅读器渲染出 2 页；Spotlight 识别为 Microsoft Word / Excel / PowerPoint document；`file --mime-type` 返回标准 OOXML MIME |

实测产出：`KaiLionCrafts-海外招商方案.docx`(10KB) · `刀具成本表.xlsx`(6.5KB) · `出海年度规划.pptx`(30KB / 21 部件) · `5Cr15MoV-厨房刀具规格表.xlsx`(8KB) · `test.pdf`(96KB / 2 页 / 2 图)

关键数据：**74 个节点 · 50 条工作流 · 14 类资源库 · 21 个工作流分类 · 11 章说明书 · 38 个隐藏节点可自定义放出 · 控制台错误 0**

---

# 第六部分：一句话总结

| 问题 | 答案 |
|---|---|
| 我是否完整读取了原工作台的功能？ | **是。** 68 个节点、20 类工作流、14 类资源库、6 大界面区域、17 个接口、全部交互规则，都已逐条掌握 |
| 我是否掌握了它的源代码与样式代码？ | **没有。** 前端源码是压缩嵌入在二进制里的，我没有做反编译提取；界面也没实际运行观察过 |
| 我的工作台是否和它的功能一样？ | **框架级 100% 同构，节点级 100% 齐全，执行深度约 40%~60%** —— 面板、流程、参数、交互规则对齐；真实模型调用、文档真导出、6 项进化内核、HTTP API、飞书机器人还没做 |
| 现在能不能用？ | **能。** 全链路可跑通（用本地推演），界面、流程、参数、说明书、测试全部就绪；配上你自己的 API Key 即可出真实产品图 |
| 下一步最关键的一件事？ | **向朋友要前端源码 + 节点定义文件。** 有这两样，我能把界面与节点能力做到接近 1:1；没有，也能继续用现在这套自研架构演进 |

---

*本报告为 KaiLionCrafts 内部技术文档。KaiLionCreator 的代码、文案、数据与视觉均为独立原创实现，未复制、未反编译提取、未复用任何第三方源码或素材。*
