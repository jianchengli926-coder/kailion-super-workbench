# 锴利超级AI工作台 - 部署说明

## 📍 当前部署位置
外置硬盘：`/Volumes/Kingston 1TB NV1 40Gbps/豆包独立站SEO项目/锴利超级AI工作台`

## 🚀 本地启动

### 方式1：双击启动（推荐）
双击 `启动工作台-局域网版.command`（Mac）或 `启动工作台-局域网版.bat`（Windows）

### 方式2：命令行启动
```bash
cd "/Volumes/Kingston 1TB NV1 40Gbps/豆包独立站SEO项目/锴利超级AI工作台"
python3 -m http.server 8766 --bind 0.0.0.0
```

## 🌐 访问地址

| 访问方式 | 地址 | 说明 |
|---------|------|------|
| 本机访问 | http://localhost:8766 | 自己电脑上打开 |
| 局域网访问 | http://192.168.1.22:8766 | 同事在同一局域网内打开 |
| 公网访问 | 待配置 | 需要域名+反向代理 |

## ⚠️ 注意事项

1. **服务器必须保持运行**：关闭终端或电脑，同事就访问不了了
2. **数据不共享**：每个用户的工作流、API配置存在自己的浏览器里
3. **API Key自己配**：同事需要自己在设置里配置API供应商
4. **本地Ollama不共享**：同事访问时，localhost指的是同事自己的电脑
5. **防火墙**：Mac可能需要在系统设置→网络→防火墙中允许8766端口

## 🔧 局域网访问配置

### Mac防火墙设置
1. 打开 系统设置 → 网络 → 防火墙
2. 确保防火墙已关闭，或添加python3例外
3. 或者使用命令行：`sudo /usr/libexec/ApplicationFirewall/socketfilterfw --add /usr/bin/python3`

### 固定IP建议
为了方便同事访问，建议给电脑设置固定IP：
1. 系统设置 → 网络 → 高级 → TCP/IP
2. 配置IPv4为"手动"
3. 设置IP为 192.168.1.22（或其他固定地址）

## 🌍 公网部署（待配置）

需要准备：
1. 一个域名（子域名）
2. 反向代理工具（ngrok / frp / Cloudflare Tunnel）
3. 或者云服务器部署

详见「公网部署方案」章节。
